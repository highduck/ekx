<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.eliasku.template_android"
          xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-feature android:glEsVersion="0x00020000" android:required="true" />
    <uses-feature android:name="android.hardware.screen.landscape" android:required="false"/>
    <uses-feature android:name="android.hardware.screen.portrait" android:required="false"/>

    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- TEMPLATE ROOT -->

    <!-- NOTE: Crashlytics NDK requires allowNativeHeapPointerTagging="false" -->
    <application
        android:allowNativeHeapPointerTagging="false"
        android:name="androidx.multidex.MultiDexApplication"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="com.eliasku.template_android.MainActivity"
            android:configChanges="PLACEHOLDER"
            android:screenOrientation="sensorPortrait"
            android:theme="@style/AppTheme.Launch"
            android:launchMode="singleTask"
            android:taskAffinity="">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- TEMPLATE APPLICATION -->
    </application>

</manifest>