{
  "name": "js-script-test",
  "version": "0.0.2",
  "private": true,
  "scripts": {
    "postinstall": "cmake-build",
    "build:web": "ekx web clean --verbose",
    "build:android": "ekx android bundle clean --verbose",
    "build:assets:dev": "ekx assets --dev",
    "clean": "rimraf ./build ./export ./.firebase"
  },
  "dependencies": {
    "@ekx/graphics": "0.1.8",
    "@ekx/cli": "0.1.11",
    "cmake-build": "0.0.4",
    "@ekx/quickjs": "0.0.1"
  },
  "devDependencies": {
    "rimraf": "^3.0.2"
  }
}
