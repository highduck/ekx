{
  "name": "@ekx/ekx",
  "version": "0.1.10",
  "license": "ISC",
  "publishConfig": {
    "access": "public"
  },
  "bin": {
    "ekx": "./cli.js",
    "sokol-shdc": "./external/sokol/sokol-shdc.js"
  },
  "scripts": {
    "release": "changeset publish",
    "version": "changeset version && yarn run update-lock",
    "update-lock": "yarn && git add yarn.lock",
    "ci:build": "deno run -A build.ts",
    "ci:build-native": "deno run -A build-native.ts"
  },
  "devDependencies": {
    "@changesets/cli": "^2.23.0",
    "esbuild": "^0.14.48",
    "@types/emscripten": "^1.39.6"
  }
}
