{
  "name": "@ekx/ekx",
  "version": "0.1.10",
  "license": "ISC",
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "release": "changeset publish",
    "version": "changeset version && yarn run update-lock",
    "update-lock": "yarn && git add yarn.lock",
    "ci:build": "deno run -A build.ts",
    "ci:native-build": "deno run -A native-build.ts",
    "ci:native-coverage": "deno run -A native-coverage.ts"
  }
}
